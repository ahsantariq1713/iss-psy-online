// geoip2.country(function (result) {
//     let country = result.registered_country.names['en'];
//     document.querySelector('meta[name="country"]').setAttribute("content", country);
// }, () => {
//     alert('error')
// });
//
// let timezone = Intl.DateTimeFormat().resolvedOptions().timeZone;
// document.querySelector('meta[name="timezone"]').setAttribute("content", timezone);
